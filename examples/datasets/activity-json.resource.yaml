kind: "Resource"
metadata:
  name: "activity-json-dataset"
spec:
  store:
    format: "jsondoc"
    location: "${JOB_DATA_BUCKET_PREFIX}/account-activity/account=all/1.json"
    partition: "account=all"
    options:
      extractArray: data
